#include <stdio.h>

/**
 * main - Entry point
 * Return: always 0
 *
 */

int main(void)
{
	int n = 97;
	int l = 65;

	while (n >= 97)
	{
		putchar (n);
	}
	while (l >= 65)
	{
		putchar (L);
	}
	putchar ('\n');
	return (0);
}

