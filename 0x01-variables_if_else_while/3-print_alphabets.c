#include <stdio.h>

/**
 * main - Entry point
 * Return: always 0
 *
 */

int main(void)
{
	int n = 97;
	int m = 65;

	while (n >= 97)
	{
		putchar (n);
	}
	while (m >= 65)
	{
		putchar (L);
	}
	putchar ('\n');
	return (0);
}

